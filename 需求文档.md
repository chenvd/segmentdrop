# Emby → TheIntroDB 快速分段提交工具需求文档

## 1. 文档状态

- 状态：第一版需求已确认
- 目标形态：独立轻量 Web 应用
- 部署方式：单 Docker 容器
- 主要使用终端：手机浏览器
- 后端技术：Python 3.12、FastAPI、Uvicorn 单 worker
- 前端技术：Jinja2、原生 JavaScript、原生 CSS
- 数据存储：不使用数据库，配置默认写入运行目录下的 `./data/config.json`

## 2. 产品目标

读取 Emby 当前正在播放的电影或剧集，利用播放器上已经定位好的播放位置，快速编辑 Intro、Recap、Credits、Preview 四类分段，并将本集或本片的有效分段提交到 TheIntroDB。

核心流程：

1. 用户在电视或播放器上播放并定位媒体。
2. 用户使用手机打开工具，从首页选择活动 Session。
3. 详情页只读取影片信息，不读取 Emby 已有分段。
4. 用户选择一种分段类型，并在播放器上定位分段边界。
5. 用户点击对应 Start 或 End 的“获取进度”。
6. 工具按需读取该 Session 的最新播放位置并填入字段。
7. 用户提交当前这一条分段，前端显示结果。

## 3. 非目标

第一版不提供：

- 播放、暂停、Seek、停止等 Emby 播放控制
- Emby 媒体库浏览
- TheIntroDB 数据浏览或已有分段查询
- 详情页实时播放状态
- WebSocket
- 用户账号系统
- 按 Emby 用户筛选 Session
- 多 Emby Server 管理
- 提交历史、草稿、任务队列和失败自动重试
- 修改或写回 Emby 分段
- 字幕级或逐帧编辑

## 4. 设计原则

- 手机优先，所有主要按钮的触控高度不低于 44px。
- 页面操作路径短，不显示实现细节，例如轮询间隔。
- 首页可以实时刷新；详情页只加载影片信息。
- 播放位置只在用户点击“获取进度”时读取。
- 所有 Emby 和 TheIntroDB API Key 只由后端使用，不返回浏览器。
- 时间在系统内部统一使用整数毫秒，避免浮点累积误差。
- 外部 API 封装在独立 service 中，不将第三方响应直接透传给前端。
- 操作成功或轻量错误使用顶部 Toast，出现位置必须叠加 CSS `env(safe-area-inset-top)` 并避开固定 Header；Toast 从上方向下进入，不使用底部提示。

## 5. 页面需求

### 5.1 全局导航

Header 固定包含：

- 产品标识
- “正在播放”入口
- “设置”入口

手机端导航只显示图标，保留可访问名称。

### 5.2 首页：活动 Session

#### 5.2.1 数据刷新

- 首页加载后立即请求一次活动 Session。
- 此后默认每 3 秒轮询一次后端 `/api/sessions`。
- 页面只显示“Emby 已连接”和简化后的同步状态，例如“刚刚同步”。
- 不向用户展示“每 3 秒刷新”等实现信息。
- 页面不可见时暂停轮询；重新可见后立即刷新一次。
- 同一次请求尚未结束时不得发起下一次轮询。

#### 5.2.2 Session 筛选

后端调用 Emby `GET /Sessions`，不传 `ControllableByUserId`、`DeviceId` 或其他用户筛选条件。

只保留同时满足以下条件的 Session：

1. `NowPlayingItem` 存在。
2. `NowPlayingItem.Type` 为 `Movie` 或 `Episode`。
3. Movie 的 `NowPlayingItem.ProviderIds` 中存在非空 TMDb ID；Episode 能通过 `SeriesId` 解析到带 TMDb ID 的 Series。

`ProviderIds` 的键按不区分大小写处理，兼容 `Tmdb`、`TMDb` 等形式。无最终可用 TMDb ID 的 Session 不显示，也不显示失败占位卡。

实机数据表明 Episode 自身通常只有 TVDb/IMDb ID，TMDb ID 位于 Series 条目上。因此首页处理 Episode 时必须按 `SeriesId` 补查 Series，不能仅判断 `NowPlayingItem.ProviderIds.Tmdb`。相同 `SeriesId` 的 TMDb 解析结果应在内存中短时缓存，避免每 3 秒重复查询。

多个用户或多个设备满足条件时全部显示，不提供用户筛选设置。

#### 5.2.3 Session 卡片

每张卡片展示：

- 海报
- 影片名；剧集优先显示 `SeriesName`
- 单集标题 `Name`
- Season / Episode
- 类型：电影或剧集
- Emby 用户名 `UserName`
- 客户端与设备，例如 `Client`、`DeviceName`
- 播放中或已暂停状态
- 当前时间、总时长和只读进度条

进度条仅展示状态：

- 不带拖动手柄
- 不响应点击或拖动
- 进度比例为 `PlayState.PositionTicks / NowPlayingItem.RunTimeTicks`
- 运行时缺失或为 0 时隐藏进度条

点击卡片进入详情页，并携带：

- `session_id`
- `item_id`

`item_id` 用于后续防止 Session 已切换到其他媒体。

#### 5.2.4 首页状态

- 尚未配置 Emby：显示设置引导。
- Emby 无法连接：显示连接失败和重试按钮。
- API Key 无效：显示认证失败和设置入口。
- 无符合条件的 Session：显示“当前没有带 TMDb ID 的电影或剧集正在播放”。
- 请求成功：状态元素显示“Emby 已连接 · 刚刚同步”。

### 5.3 详情页

#### 5.3.1 加载行为

进入详情页只加载一次：

- 影片或剧集元数据

详情页不轮询，不展示当前播放位置、实时播放状态、Session 在线状态或播放进度。

顶部影片区展示：

- 海报
- 电影名，或剧名与单集标题
- Season / Episode
- 年份
- 电影或电视剧
- 最终用于提交的 TMDb ID
- Emby Item ID，可作为调试信息在第一版保留

页面始终展示“本次提交预览”、媒体总时长和灰色轨道。彩色条只表示当前表单中这一条分段在媒体时长中的位置，不表示当前播放进度，也不可拖动。必填边界存在时即可绘制：Intro、Recap 的 Start 为空时从媒体开头绘制，Credits、Preview 的 End 为空时绘制到媒体结尾。必填边界缺失时保留预览区和灰色轨道，但不显示彩色条，也不显示空状态或非法提示。

前端只按片段类型检查必填与可选字段，并据此启用或灰化提交按钮。Start、End 顺序、片段时长范围和媒体总时长等业务校验由后端负责，前端不重复实现。

详情页在宽度不小于 960px 的桌面视口使用左右两栏：左侧为影片信息和分段预览，右侧为类型选择、时间输入和提交操作。两栏按各自内容自然高度展示，不强制拉伸影片卡或海报。较窄视口继续按相同内容顺序上下排列。

#### 5.3.2 TMDb ID 解析

电影：

- 使用 `NowPlayingItem.ProviderIds.Tmdb` 作为最终电影 TMDb ID。

剧集：

- TheIntroDB 提交需要剧集所属 Series 的 TMDb ID，以及 Season、Episode 编号。
- 使用 `NowPlayingItem.SeriesId` 查找 Series 条目。
- 使用当前 Session 的 `UserId` 调用 `GET /Users/{UserId}/Items/{SeriesId}`。
- 从 Series 的 `ProviderIds.Tmdb` 获取最终 Series TMDb ID。
- Season 使用 Episode 的 `ParentIndexNumber`。
- Episode 使用 Episode 的 `IndexNumber`。

这里使用 Session 自带的 `UserId` 只是为了读取该用户可见的 Series 条目，不代表提供用户筛选功能。

如果无法解析 Series、Season、Episode 或 Series TMDb ID，则详情页显示明确错误并禁止提交。

#### 5.3.3 Session 初次校验

详情页加载时通过 Emby `GET /Sessions?Id={session_id}` 重新读取目标 Session，并验证：

1. Session 仍存在。
2. `NowPlayingItem` 仍存在。
3. `NowPlayingItem.Id` 与页面携带的 `item_id` 相同。

验证通过后读取影片详情。验证失败时不加载错误媒体的数据。

### 5.4 分段编辑

先通过四个并列按钮选择一种类型：

- Intro
- Recap
- Credits
- Preview

下方只显示当前类型的一组分段输入：

- Start 输入框
- End 输入框
- Start “获取进度”按钮
- End “获取进度”按钮
- Start、End 各自的 `-1s`、`-0.1s`、`+0.1s`、`+1s`
- “清空时间”按钮

详情页始终从空表单开始，不读取或预填 Emby Chapters。一次只编辑并提交一个区间；若需提交其他类型或另一个区间，完成当前提交后再次选择并填写。

“清空时间”会清除当前 Start 与 End：

- 只修改浏览器中的表单状态，不修改 Emby。
- 存在任意值时显示；完全没有值时隐藏。

显示格式统一为：

```text
HH:MM:SS.s
```

内部格式统一为：

```json
{
  "start_ms": null,
  "end_ms": 168000
}
```

输入框支持手动输入。失去焦点时进行解析和规范化显示。

根据 TheIntroDB v3 当前规则，四种类型的必填边界不同：

| 类型 | Start | End | 空值语义 |
| --- | --- | --- | --- |
| Intro | 可选 | 必填 | Start 为空表示从媒体开头开始 |
| Recap | 可选 | 必填 | Start 为空表示从媒体开头开始 |
| Credits | 必填 | 可选 | End 为空表示持续到媒体结束 |
| Preview | 必填 | 可选 | End 为空表示持续到媒体结束 |

界面必须在可选字段旁及空输入框中显示“可为空”，不能使用“留空表示从开头/到结尾”等容易让用户误以为存在页面默认值的提示。必填字段没有数据时保持空白，不显示 `00:00:00.0` placeholder。空值不能自动转换成 `00:00:00.0`。提交给 TheIntroDB 时可选空值显式发送为 JSON `null`。

TheIntroDB 支持 Start、End 同时为 `null`，语义为“无此分段”。本工具不提供“无分段”贡献功能；两个字段都为空时不提交。

### 5.5 Emby 分段读取规则

详情接口不请求 `Chapters`，也不解析或返回 Emby 已有分段。已有分段可能来自 TheIntroDB 插件同步，拿它们预填再提交容易产生重复贡献；本工具只使用用户本次主动获取或填写的时间。

### 5.6 获取播放位置

详情页不自动更新播放位置。用户点击某个 Start 或 End 的“获取进度”按钮时，前端请求：

```text
GET /api/sessions/{session_id}/position?item_id={item_id}
```

后端调用 Emby：

```text
GET /Sessions?Id={session_id}
```

并验证：

1. Session 仍存在。
2. `NowPlayingItem` 仍存在。
3. `NowPlayingItem.Id` 等于 `item_id`。
4. `PlayState.PositionTicks` 存在且为非负数。

成功后将 `PositionTicks / 10,000` 转为整数毫秒，填入用户点击的唯一目标字段。

错误文案：

- Session 已停止播放：`当前设备已经停止播放该影片。`
- Session 已切换媒体：`当前播放内容已经改变，请返回正在播放页面重新选择。`
- 播放位置缺失：`暂时无法读取当前播放位置，请稍后重试。`
- Emby 不可访问：`无法连接 Emby，请检查服务器状态。`

是否处于暂停状态不影响“获取进度”。播放中和暂停时都允许读取 `PositionTicks`。

### 5.7 提交

详情页底部使用手机端友好的固定提交栏：

```text
提交本集片头
```

按钮文案中的类型随选择变化。提交规则：

- Intro、Recap：End 必填；Start 可为空，表示从媒体开头开始。
- Credits、Preview：Start 必填；End 可为空，表示持续至媒体结束。
- 两个字段都为空时不提交。
- 只填写部分且不符合该类型规则时显示明确错误。

后端校验：

- 类型必须为 `intro`、`recap`、`credits`、`preview` 之一。
- 非空时间必须为整数毫秒且不小于 0。
- Intro、Recap 的 End 必填；Start 为空时按 0 参与时长校验。
- Credits、Preview 的 Start 必填；End 可以为空。
- Start、End 同时存在时 End 必须大于 Start。
- Intro 时长必须为 5 至 200 秒。
- Recap 时长必须为 5 至 1200 秒。
- Credits、Preview 提供 End 时，时长必须为 5 至 1800 秒。
- 所有非空时间戳最大为 21600000ms，即 6 小时。
- 向 TheIntroDB 提交 `video_duration_ms` 时，其值必须在 300000 至 21600000ms 之间。
- 时间不得超过媒体时长；允许少量由播放器上报误差导致的容差，建议 1 秒。
- Movie 必须有电影 TMDb ID。
- Episode 必须有 Series TMDb ID、Season 和 Episode。

前端一次只发送一个 `segment`，后端一次只调用一次 TheIntroDB `POST /v3/submit`，不接受多分段批量请求。

结果状态：

- 成功
- 重复提交
- 参数无效
- API Key 无效
- 网络错误
- TheIntroDB 服务端错误

重复提交显示为中性状态，不显示为严重失败。TheIntroDB API Key 无效时提供设置入口。

TheIntroDB 的外部 API 版本和提交路径封装在 `TheIntroDBService` 内。当前官方接口为：

```text
POST https://api.theintrodb.org/v3/submit
Authorization: Bearer {api_key}
Content-Type: application/json
```

提交时间统一使用 `start_ms`、`end_ms`，两者都必须出现在 JSON 中，但允许按类型规则取 `null`。TheIntroDB 请求中的 `type` 表示媒体类型（`movie` 或 `tv`），`segment` 表示分段类型（`intro`、`recap`、`credits`、`preview`）。不得在同一请求中混用秒和毫秒字段。前端和本项目内部 API 不依赖第三方版本路径，并为外部适配器添加契约测试。

## 6. Settings

设置页只包含：

### Emby

- Server URL
- API Key
- 测试连接
- 保存 Emby 设置

### TheIntroDB

- API Key
- 测试 API Key
- 保存 TheIntroDB 设置

不提供用户筛选字段。

`GET /api/settings` 只返回 Server URL，以及 API Key 是否已配置或脱敏状态，不返回完整密钥。

配置文件示例：

```json
{
  "emby": {
    "server_url": "http://192.168.1.8:8096",
    "api_key": "..."
  },
  "theintrodb": {
    "api_key": "..."
  }
}
```

写入采用同目录临时文件加原子替换：

```text
./data/config.json.tmp → ./data/config.json
```

## 7. 本项目后端接口

```text
GET  /api/sessions
GET  /api/sessions/{session_id}?item_id={item_id}
GET  /api/sessions/{session_id}/position?item_id={item_id}

POST /api/submit

GET  /api/settings
PUT  /api/settings
POST /api/settings/test-emby
POST /api/settings/test-theintrodb

GET  /api/images/{item_id}/primary
GET  /healthz
```

### 7.1 `GET /api/sessions`

只返回前端需要的归一化数据：

```json
[
  {
    "session_id": "session-1",
    "item_id": "84321",
    "item_type": "episode",
    "title": "羊毛战记",
    "episode_title": "路障",
    "season": 2,
    "episode": 6,
    "user_name": "Chris",
    "client": "Emby for Apple TV",
    "device_name": "Apple TV 客厅",
    "is_paused": true,
    "position_ms": 1182300,
    "duration_ms": 3118000,
    "progress_percent": 37.92,
    "poster_url": "/api/images/84321/primary"
  }
]
```

### 7.2 详情响应

```json
{
  "session_id": "session-1",
  "item_id": "84321",
  "item_type": "episode",
  "title": "羊毛战记",
  "episode_title": "路障",
  "season": 2,
  "episode": 6,
  "year": 2024,
  "tmdb_id": 125988,
  "duration_ms": 3118000,
  "poster_url": "/api/images/84321/primary"
}
```

### 7.3 提交请求

```json
{
  "session_id": "session-1",
  "item_id": "84321",
  "media": {
    "type": "episode",
    "tmdb_id": 125988,
    "season": 2,
    "episode": 6,
    "duration_ms": 3118000
  },
  "segment": { "type": "intro", "start_ms": 91000, "end_ms": 168000 }
}
```

后端不得完全信任前端提供的媒体身份。提交前至少校验请求中的 `item_id`、媒体类型和服务端详情加载时可确认的标识格式。

## 8. Emby API 字段清单

### 8.1 `SessionInfo`

第一版使用：

- `Id`
- `UserId`
- `UserName`
- `Client`
- `DeviceName`
- `LastActivityDate`
- `NowPlayingItem`
- `PlayState.PositionTicks`
- `PlayState.IsPaused`

### 8.2 `NowPlayingItem` / `BaseItemDto`

第一版使用：

- `Id`
- `Type`
- `Name`
- `SeriesName`
- `SeriesId`
- `IndexNumber`
- `ParentIndexNumber`
- `ProductionYear`
- `RunTimeTicks`
- `ProviderIds`
- `ImageTags`
- `SeriesPrimaryImageTag`

Emby 官方文档说明 `ProviderIds` 是第三方元数据 ID 字典，Episode 的 `SeriesId` 指向所属 Series；`ParentIndexNumber` 对 Episode 表示季号，`IndexNumber` 表示集号。

### 8.3 认证

- 使用 Emby API Key，优先通过 `X-Emby-Token` Header 发送。
- API Key 适合无交互的集成服务。
- Server URL 统一去除末尾 `/`，内部组合为 `{server_url}/emby/{api_path}`。
- 日志不得记录 Header、API Key、完整配置对象或含密钥的 URL。

## 9. 错误处理

后端统一输出：

```json
{
  "error": {
    "code": "EMBY_UNREACHABLE",
    "message": "无法连接 Emby，请检查服务器状态。"
  }
}
```

建议错误码：

- `SETTINGS_REQUIRED`
- `EMBY_UNREACHABLE`
- `EMBY_UNAUTHORIZED`
- `SESSION_NOT_FOUND`
- `SESSION_STOPPED`
- `SESSION_ITEM_CHANGED`
- `POSITION_UNAVAILABLE`
- `TMDB_ID_MISSING`
- `SERIES_TMDB_ID_MISSING`
- `SEGMENT_INVALID`
- `THEINTRODB_UNAUTHORIZED`
- `THEINTRODB_DUPLICATE`
- `THEINTRODB_UNREACHABLE`
- `THEINTRODB_SERVER_ERROR`

## 10. 非功能需求

### 性能

- Uvicorn 单 worker。
- 全局复用一个 `httpx.AsyncClient`。
- Emby 和 TheIntroDB 请求分别设置连接与响应超时。
- 不使用 Redis、Celery、PostgreSQL 或 SQLite。
- 首页轮询响应不包含不需要的大字段。

### 安全

- 容器使用非 root 用户。
- Docker 将 `/app/data` 通过 volume 映射到宿主机的 `./data`，配置文件权限限制为容器用户可读写。
- 设置修改请求校验 CSRF Token 或严格校验 `Origin`。
- 如果暴露到局域网以外，必须由反向代理提供 HTTPS 和访问认证。
- 海报通过本项目后端代理，浏览器不得直接获得 Emby API Key。

### Docker

- 基础镜像：`python:3.12-slim`
- 默认端口：`8000`
- 容器持久化目录：`/app/data`，建议映射到宿主机 `./data`
- 健康检查：`GET /healthz`
- 建议内存限制：`128M` 起，根据实际镜像和运行数据调整到 `256M`

## 11. 验收标准

1. 设置中不存在用户筛选字段。
2. 多个 Emby 用户的有效 Session 可以同时显示。
3. 首页只显示带 TMDb ID 的 Movie 和 Episode。
4. 首页轮询不会重叠发送请求，页面隐藏时停止轮询。
5. 首页进度条不可点击、不可拖动且比例正确。
6. 详情页进入后不自动请求播放位置，也不轮询 Session。
7. 详情页只展示影片信息和一条空白分段表单，不请求或预填 Emby Chapters。
8. 页面通过四个并列按钮选择 Intro、Recap、Credits 或 Preview，再只展示一组 Start、End 输入。
9. 切换类型时 Start、End 的必填与可为空提示正确变化。
10. 后端提交接口只接受单个 `segment`，每次最多调用一次 TheIntroDB。
11. 点击“获取进度”会重新校验 Session 和 Item ID。
12. 播放中或暂停时都能获取当前位置。
13. Session 停止或切换媒体时，不会把错误位置写入表单。
14. 时间微调始终以 100ms 或 1000ms 的整数步长计算。
15. 一次操作只提交一个分段，并显示该条结果。
16. API Key 无效时显示明确错误。
17. 浏览器响应中、页面源代码和日志中不出现完整 API Key。
18. `config.json` 在容器重启后仍然有效。
19. 主要流程在宽度 390px 的手机视口无横向滚动。
20. 详情页在宽度不小于 960px 时使用左右双栏布局。

## 12. 实机验证结果

验证环境：用户提供的 Emby Server，只读查询日期为 2026-08-25。

| 媒体库 | 检查条目 | 带 TheIntroDB Marker 的条目 | 完整 Intro | 完整 Credits | 完整 Recap | 完整 Preview |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 电视剧 | 117 | 69 | 51 个条目 / 68 对 | 25 | 4 | 0 |
| 节目 | 19 | 0 | 0 | 0 | 0 | 0 |
| 番 | 268 | 246 | 233 | 227 | 0 | 4 |

确认结果：

- 以下结果只保留为历史调研记录；当前详情页不会读取这些 `Chapters`。
- TheIntroDB 所定义的 Intro、Recap、Credits、Preview 四种分段都能在 Emby `Chapters` 中出现。
- 四种类型不保证每个媒体都有；取决于 TheIntroDB 数据和同步结果。
- Intro、Credits 使用 Emby 标准 MarkerType，并可能附带同时间的普通 Chapter。
- Recap、Preview 使用带 TheIntroDB 签名的普通 Chapter。
- 实机 Episode 条目自身没有 TMDb ID，但对应 Series 条目有 TMDb ID；Session 筛选和提交必须解析 Series。
- 存在同一集多个 Intro 的情况，不能假设每种类型永远只有一个区间。

## 13. 官方资料

- [Emby REST API 首页](https://dev.emby.media/doc/restapi/index.html)
- [Emby `GET /Sessions`](https://dev.emby.media/reference/RestAPI/SessionsService/getSessions.html)
- [Emby Item Information](https://dev.emby.media/doc/restapi/Item-Information.html)
- [Emby Browsing the Library](https://dev.emby.media/doc/restapi/Browsing-the-Library.html)
- [TheIntroDB 官方网站](https://theintrodb.org/)
- [TheIntroDB 官方 Emby 插件](https://github.com/TheIntroDB/emby-plugin)
